[build-system]
requires = [
    "grpcio-tools >= 1.47, < 2",
    "protobuf >= 3.19, < 4",
    "setuptools >= 60.10.0, < 62",
    "setuptools_scm[toml] >= 6.4.2, < 7",
    "wheel"
]
build-backend = "setuptools.build_meta"
